<button type="button" class="theme-toggle" data-theme-toggle aria-pressed="false" hidden>
  <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="8.5" /><path d="M12 3.5a8.5 8.5 0 0 1 0 17Z" /></svg>
  <span>Dark mode</span>
</button>
<style>
  .theme-toggle { display: inline-flex; align-items: center; justify-content: center; gap: 9px; min-height: 44px; padding: 0 4px; border: 0; border-radius: 0; background: transparent; color: inherit; font: 400 13px 'Space Grotesk', sans-serif; white-space: nowrap; cursor: pointer; touch-action: manipulation; -webkit-tap-highlight-color: transparent; }
  .theme-toggle[hidden] { display: none; }
  .theme-toggle svg { width: 20px; height: 20px; fill: none; stroke: currentColor; stroke-width: 1.5; }
  .theme-toggle path { fill: currentColor; stroke: none; }
  .theme-toggle:hover, .theme-toggle[aria-pressed='true'] { color: var(--theme-toggle-accent, var(--site-accent)); }
  .theme-toggle:focus-visible { outline: 3px solid currentColor; outline-offset: 4px; }
  @media (max-width: 760px) { .theme-toggle { font-size: 11px; gap: 6px; padding-inline: 0; } .theme-toggle svg { width: 18px; height: 18px; } }
</style>
